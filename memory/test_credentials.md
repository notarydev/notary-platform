# Test credentials / access
- Local preview: NOTARY_API_AUTH_TOKEN is UNSET → `/v1` auth is DISABLED (no token needed).
- Production (api.getnotary.ai) demo token supplied by user:
  Authorization: Bearer ntry-demo-57272d3e2abd52fcd6b74325043a1b53
- App URL (preview): <REACT_APP style not used>; served by FastAPI at /app and /v1 on same origin.
