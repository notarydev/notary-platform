# Infrastructure

This directory contains Terraform scaffolding for the Notary Platform AWS footprint.

The initial modules are intentionally minimal and are expected to be expanded with environment-specific variables, IAM policies, networking, observability, and deployment automation.
